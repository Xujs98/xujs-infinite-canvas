services:
  app:
    image: julong-canvas:local
    container_name: julong-canvas-local
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "${APP_PORT:-3001}:3000"
    restart: unless-stopped
